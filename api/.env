# .env文件内容（复制粘贴即可，根据你的本地MySQL配置修改）
# 数据库配置（核心！）
DB_HOST=localhost
DB_PORT=3306
DB_USER=root          # 本地MySQL用户名（默认root）
DB_PASSWORD=Mwd20110915    # 你本地MySQL的密码（安装时设置的）
DB_NAME=score_db      # 数据库名（自动创建）

# JWT密钥（自定义即可）
SECRET_KEY=pOAIi4ZR2aVxQ3KB-n9PSY5rMrxW0pA39sP32ZQuEyk

# 其他配置
ENV=dev
TOKEN_EXPIRE_HOURS=24
BCRYPT_ROUNDS=12
LOCK_THRESHOLD=5
LOCK_WINDOW_SECONDS=300
ATTEMPT_WINDOW=600

# Redis（本地运行可留空，禁用缓存）
REDIS_HOST=
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=
REDIS_SSL=False