<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <title><%= title %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <style>
    :root {
      --primary: #1677ff;
      --success: #52c41a;
      --warning: #faad14;
      --bg: #f0f2f5;
      --card: #ffffff;
      --text-main: #1f1f1f;
      --text-sub: #6b7280;
      --border: #e5e7eb;
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      min-height: 100vh;
      font-family: -apple-system, BlinkMacSystemFont, "Microsoft YaHei", "Segoe UI", sans-serif;
      background: var(--bg);
      color: var(--text-main);
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .page {
      width: 100%;
      max-width: 960px;
      padding: 20px;
    }

    .card {
      background: var(--card);
      border-radius: 14px;
      padding: 36px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.08);
    }

    .header {
      text-align: center;
      margin-bottom: 32px;
    }

    .header h1 {
      margin: 0;
      font-size: 28px;
      font-weight: 600;
    }

    .header p {
      margin-top: 10px;
      font-size: 14px;
      color: var(--text-sub);
    }

    .stats {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 20px;
      margin-bottom: 36px;
    }

    .stat-item {
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 20px;
      text-align: center;
    }

    .stat-title {
      font-size: 14px;
      color: var(--text-sub);
      margin-bottom: 10px;
    }

    .stat-value {
      font-size: 24px;
      font-weight: 600;
    }

    .ok {
      color: var(--success);
    }

    .warn {
      color: var(--warning);
    }

    .actions {
      display: flex;
      justify-content: center;
      gap: 16px;
      flex-wrap: wrap;
    }

    .btn {
      padding: 10px 22px;
      border-radius: 8px;
      font-size: 14px;
      text-decoration: none;
      transition: 0.2s;
    }

    .btn-primary {
      background: var(--primary);
      color: #fff;
    }

    .btn-primary:hover {
      opacity: 0.9;
    }

    .btn-outline {
      border: 1px solid var(--border);
      color: var(--text-main);
      background: #fff;
    }

    .btn-outline:hover {
      background: #f5f5f5;
    }

    footer {
      margin-top: 28px;
      text-align: center;
      font-size: 12px;
      color: var(--text-sub);
    }
  </style>
</head>

<body>

  <div class="page">
    <div class="card">

      <div class="header">
        <h1><%= systemName %></h1>
        <p>
          运行环境：<%= env %> ｜ 当前时间：<%= now %>
        </p>
      </div>

      <div class="stats">
        <div class="stat-item">
          <div class="stat-title">服务状态</div>
          <div class="stat-value ok"><%= healthStatus %></div>
        </div>

        <div class="stat-item">
          <div class="stat-title">登录状态</div>
          <div class="stat-value <%= user ? 'ok' : 'warn' %>">
            <%= user ? '已登录' : '未登录' %>
          </div>
        </div>

        <div class="stat-item">
          <div class="stat-title">系统时间</div>
          <div class="stat-value" id="clock">--:--:--</div>
        </div>
      </div>

      <div class="actions">
        <a href="/login" class="btn btn-primary">登录系统</a>
        <a href="/api/health" class="btn btn-outline">健康检查</a>
        <a href="/admin" class="btn btn-outline">后台管理</a>
      </div>

      <footer>
        © <%= new Date().getFullYear() %> <%= systemName %>
      </footer>

    </div>
  </div>

  <script>
    function updateClock() {
      const now = new Date();
      document.getElementById('clock').textContent =
        now.toLocaleTimeString();
    }
    updateClock();
    setInterval(updateClock, 1000);
  </script>

</body>
</html>
